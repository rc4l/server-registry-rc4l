# data/

The four IP lists the registry reads, re-read every 15 minutes (no restart needed).

Empty by design: running an independent registry means inheriting nobody else's ban decisions.

See the moderation policy in the repo README before adding anything — entries are IP addresses,
which are personal data, and home addresses get reassigned.
